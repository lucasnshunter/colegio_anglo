<p id="notice"><%= notice %></p>



<%= link_to 'Nova Historia', new_acontecimento_path ,class:"btn btn-primary"%>
<hr>
<div class="panel panel-primary">
  <div class="panel-heading">
    <h3 class="panel-title">Historia do colégio</h3>
  </div>
  <div class="panel-body">
    <table class="table">
  <thead>
    <tr>
      <th>Titulo</th>
      <th>Texto</th>
      <th>Link</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @acontecimentos.each do |acontecimento| %>
      <tr>
        <td><%= acontecimento.titulo %></td>
        <td><%= acontecimento.texto %></td>
        <td><%= acontecimento.link %></td>
        <td><%= link_to 'Mostrar', acontecimento ,class:"btn btn-primary"%></td>
        <td><%= link_to 'Editar', edit_acontecimento_path(acontecimento),class:"btn btn-warning" %></td>
        <td><%= link_to 'Apagar', acontecimento, method: :delete, data: { confirm: 'Are you sure?' },class:"btn btn-danger" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
  </div>
</div>