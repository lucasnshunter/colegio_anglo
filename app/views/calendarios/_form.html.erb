
<div class="panel panel-primary">
  <div class="panel-heading">
    <h3 class="panel-title">Cadastrar calendario</h3>
  </div>
  <div class="panel-body">
    <%= form_for @calendario, :html => { multipart: true } do |f| %>

  <% if @calendario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@calendario.errors.count, "error") %> prohibited this calendario from being saved:</h2>

      <ul>
      <% @calendario.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %><br>
    <%= f.text_field :titulo %>
  </div>
  <div class="field">
    <%= f.label :ano %><br>
    <%= f.text_field :ano %>
  </div>
  <%= f.file_field :photo %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
  </div>
</div>